var myItem = [20, 30, 40, 50, 60,] // an array: helps us to store multiple values in a single memory space.
console.log(myItem[0]); // Indexing array values; it always start from 0: Index is simply calling out the positions of values stored in an array
    for (let i = 0; i <= 4; i++){
        // console.log(myItem[i]);
        if (myItem[i] % 2 == 0){
            console.log(myItem[i]);
        }
    }
    