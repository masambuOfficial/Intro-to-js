let num1 = 23, num2 = 67; // Statement: complete instruction to the computer

function add (){ // a named group of instruction that performs more than one function
    let answer = num1 + num2;
    console.log(answer);
}

add(); // funstion call | envocation

